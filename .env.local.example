# Copy this to .env.local and set your R2 URL
# For local development with R2:
NEXT_PUBLIC_MEDIA_BASE_URL=https://your-r2-url.r2.dev

# Optional: Cloudflare Worker IPFS proxy (edge cache + gateway fallback).
# Deploy the worker from ipfs-proxy-worker/ then set this to your worker URL (no trailing slash).
# NEXT_PUBLIC_IPFS_PROXY=https://ipfs-proxy.YOUR_USERNAME.workers.dev

# For Cloudflare Pages deployment (root domain):
# NEXT_PUBLIC_BASE_PATH=

# For Cloudflare Pages deployment (set your R2 URL):
# NEXT_PUBLIC_MEDIA_BASE_URL=https://your-r2-url.r2.dev
